#刷新地址（更改需重启后生效）
refresh_url: http://localhost:${server.port}

server:
  port: 9898

spring:
  application:
    name: config-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://47.110.236.10:8910/eureka/


---
server:
  port: 5566
spring:
  profiles: dev
hello: Test:tttttttttttttt----Hello My World from port:${server.port}